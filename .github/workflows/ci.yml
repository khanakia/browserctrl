# Code generated by volt v0.0.0-dev.13659c3 — DO NOT EDIT.
#
# Regenerate:  volt gen
# To change:   edit .volt.yml, or the template in volt itself — edits here are lost.
# volt:hash 4d9105b82414
name: ci
# Trigger changed from the volt template on purpose and PERMANENTLY: CI is
# never automated in this repo. Nothing runs on push, pull request or a
# schedule. The local `task check` / `volt ci` is the gate; this workflow
# re-runs the same gate on a clean runner ON DEMAND ONLY:
#   Actions tab → ci → Run workflow, or
#   gh workflow run ci.yml --repo khanakia/browserctrl --ref main
# `volt gen` will report this file as hand-edited and refuse to overwrite it;
# that is expected — if you ever `volt gen --force`, restore this trigger and
# this comment (the template's `on: [push, pull_request]` is a regression).
on: [workflow_dispatch]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      # go-version-file reads go.mod; GOTOOLCHAIN=auto (default) lets each
      # module pull the toolchain its own go directive names. Never write a
      # Go version literal here — it drifts from go.mod.
      - uses: actions/setup-go@v6
        with: { go-version-file: go.mod }
      - uses: khanakia/volt-action@v1
        with: { args: ci }
